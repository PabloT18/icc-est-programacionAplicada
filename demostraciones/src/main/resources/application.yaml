database:
  url: "jdbc:h2:mem:campus;DB_CLOSE_DELAY=-1"
  user: "sa"
  password: ""
# Para PostgreSQL usa PAP_DB_URL, PAP_DB_USER y PAP_DB_PASSWORD.
# Este YAML se lee explícitamente con Config.java; no interviene Spring.
